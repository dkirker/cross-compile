NAME = tisgxgfx

include ../../../support/staging.mk

ifeq (${ARCH},armv7)
include ../../../support/stage-rootfs.mk
endif

ifeq (${ARCH},armv6)
include ../../../support/stage-rootfs.mk
endif

stage-local::

clobber::

